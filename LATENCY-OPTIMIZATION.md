# 🚀 Оптимизация задержки

## Текущие настройки для минимальной задержки

### Разрешение: 1280x720 (720p)
- **Почему 720p?** 
  - Хороший баланс между качеством и задержкой
  - Меньше данных для передачи = меньше задержка
  - Достаточно для большинства задач

### Битрейт: 1 Mbps
- **Почему 1 Mbps?**
  - Оптимально для 720p с низкой задержкой
  - Достаточно для качественного видео
  - Меньше задержка буферизации

### Частота кадров: 30 FPS
- **Почему 30 FPS?**
  - Достаточно для плавного видео
  - В 2 раза меньше данных, чем 60 FPS
  - Меньше нагрузка на сеть

## Как изменить настройки

### Для еще меньшей задержки (но ниже качество):

**В файле `renderer.html`:**

1. **Уменьшить разрешение:**
```javascript
async function getDesktopSourceStream(sourceID, width = 854, height = 480, ...)
// 480p - еще меньше задержка, но заметно ниже качество
```

2. **Уменьшить битрейт:**
```javascript
const VIDEO_BITRATE = 500000; // 500 kbps - минимальная задержка
```

3. **Уменьшить FPS:**
```javascript
minFrameRate = 24, maxFrameRate = 24 // 24 FPS
```

### Для лучшего качества (но больше задержка):

1. **Увеличить разрешение:**
```javascript
async function getDesktopSourceStream(sourceID, width = 1920, height = 1080, ...)
// 1080p - лучше качество, но больше задержка
```

2. **Увеличить битрейт:**
```javascript
const VIDEO_BITRATE = 2000000; // 2 Mbps для 1080p
```

3. **Увеличить FPS:**
```javascript
minFrameRate = 60, maxFrameRate = 60 // 60 FPS
```

## Рекомендации по настройкам

| Сценарий | Разрешение | Битрейт | FPS | Задержка |
|----------|-----------|---------|-----|----------|
| Минимальная задержка | 854x480 | 500 kbps | 24 | ~50-100ms |
| **Оптимально (текущее)** | **1280x720** | **1 Mbps** | **30** | **~100-200ms** |
| Сбалансированно | 1280x720 | 1.5 Mbps | 30 | ~150-250ms |
| Высокое качество | 1920x1080 | 2 Mbps | 30 | ~200-300ms |
| Максимальное качество | 1920x1080 | 4 Mbps | 60 | ~300-500ms |

## Дополнительные оптимизации

### Уже включено:
- ✅ Trickle ICE - быстрая установка соединения
- ✅ Оптимизированный SDP - правильный битрейт
- ✅ Минимальные задержки в коде

### Можно добавить (если нужно):
- STUN серверы для лучшего NAT traversal
- Адаптивный битрейт в зависимости от сети
- Приоритизация видео пакетов

## Измерение задержки

Для измерения задержки можно:
1. Использовать инструменты разработчика браузера (Network tab)
2. Сравнить время на сервере и клиенте
3. Использовать специальные инструменты для WebRTC

Текущие настройки оптимизированы для минимальной задержки при сохранении приемлемого качества.

